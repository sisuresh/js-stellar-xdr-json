



[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
yanked = "warn"
ignore = [
]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "Unicode-DFS-2016",
]
confidence-threshold = 0.8
exceptions = [
]

[licenses.private]
ignore = true
registries = [
]

[bans]
multiple-versions = "deny"
wildcards = "deny"
allow-wildcard-paths = true
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"
allow = [
]
deny = [
]
# SPIKE (p28-cap-0084): git-pinned stellar-xdr pulls crate-git-revision 0.0.9 while
# crates.io stellar-strkey pulls 0.0.6. Remove when the pin flips to version=.
skip = [{ name = "crate-git-revision", version = "0.0.6" }]
skip-tree = [
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
# SPIKE (p28-cap-0084): stellar-xdr is git-pinned to rs-stellar-xdr#549 until it
# releases on crates.io. Revert to allow-git = [] when the pin flips to version=.
allow-git = ["https://github.com/stellar/rs-stellar-xdr"]

[sources.allow-org]
github = []

[output]
feature-depth = 1

[graph]
no-default-features = false
all-features = true
exclude = []
targets = [
    { triple = "wasm32-unknown-unknown" }
]
